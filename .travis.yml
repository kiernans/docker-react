language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t nukabunny/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nukabunny/docker-react npm run test
